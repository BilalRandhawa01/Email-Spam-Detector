# Email-Spam-Detector
This project is a Machine Learning–based Email Spam Detection System that automatically classifies emails as Spam or Not Spam (Ham). The model is trained on labeled email datasets and uses Natural Language Processing (NLP) techniques to analyze the content of emails.
